@model IEnumerable<Page>

    @foreach(Page item in Model)
    {
        <li class="nav-item">
            <a class="nav-link text-dark" asp-action="Page" asp-controller="Page" asp-route-slug="@item.Slug">@item.Title</a>
        </li>
    }